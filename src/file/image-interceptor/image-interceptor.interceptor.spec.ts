import { ImageInterceptor } from './image-interceptor.interceptor';

describe('ImageInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ImageInterceptor()).toBeDefined();
  });
});
